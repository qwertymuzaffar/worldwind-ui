## What and why

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] Kit change has unit tests; adapters and demos updated where the feature is user-facing
- [ ] `npm test`, `npm run lint` and `npm run typecheck` pass locally
- [ ] Docs updated (guide page, README table) when the public API changed
- [ ] Changeset added (`npm run changeset`) when a published package changed
