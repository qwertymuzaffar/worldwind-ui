---
'worldwind-kit': patch
'react-worldwind': patch
'ngx-worldwind': patch
---

The shared widget stylesheet now keeps panels inside the viewport on small screens, wraps the coordinates readout and lets a long layer switcher scroll. Doc comments gained categories and examples, which show up in editor tooltips.
