# @angular/build has an optional peer dependency on vitest 4 while this repo uses vitest 5.
# npm 10 crashes on that optional conflict ("Cannot read properties of null (reading 'edgesOut')")
# instead of skipping it, so peer resolution uses the legacy behaviour. Every peer this repo
# actually needs is listed explicitly in the workspace package.json files.
legacy-peer-deps=true
